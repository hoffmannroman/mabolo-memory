# hosts

One profile per machine, plus its topics.

## Entries

* [Build server](build-server.md) - Four cores, 4 GB of RAM, runs the nightly build
* [Gateway](gateway.md) - Terminates TLS and routes to the application hosts, nothing else runs on it
