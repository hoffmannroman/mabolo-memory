id: session-index-without-a-project
note: no project is active, so the project entries lose their claim and the pin keeps its
tier: hint
state:
  as_of: 2026-09-18
expect:
  in_payload: [working-hours, build-server]
  not_in_payload: [atlas-tone]
