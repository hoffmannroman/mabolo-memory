id: secrets-by-alias
note: names the entry by its alias, so a rename that keeps the alias still passes
query: where are keys and tokens kept?
expect:
  entries: [secrets]
  rank_within: 1
tier: index
